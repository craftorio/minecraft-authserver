project_id = "minecraft-authserver"

[api]
# The Kong gateway exposed by docker-compose listens on port 8000.
port = 8000

[db]
# Connect through the Supavisor pooler exposed on port 5432.
port = 5432
major_version = 15

[studio]
# Studio runs on port 3000 in the compose stack.
port = 3000

[auth]
jwt_secret = "supabase_jwt_secret"
